extends layout

block content
  div
    h2 #[span.heading コメントページ ]
    p ご意見があればいただけると、助かります！
    form(action="/comment" method="post")
      span コメント: #[input(type="text" name="comment" required)] #[button(type="submit") コメントする]
      br
      br
      
    ul
      each val in commentList
        li #{val.comment} |  #{val.created_at}
      
      
