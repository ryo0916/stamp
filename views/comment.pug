extends layout

block content
  div
    h2 コメントページ 
  
  div(class='comment-form')
    form(action="/comment" method="post")
      p #[input(id="comment-input" type="text" name="comment" required)] #[button(class="button" type="submit") コメントする]
        
    ul
      each val in commentList.reverse()
        li
          span(class='comment-li') #{val.comment} | #{val.created_at}

      
